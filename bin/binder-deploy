#!/usr/bin/env node
var program = require('commander')

var binderProtocol = require('binder-protocol')

var generateCLI = require('../lib/generate-cli.js')

generateCLI(program, 'deploy', binderProtocol)
program.parse(process.argv)
